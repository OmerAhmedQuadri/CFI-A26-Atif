//48
//What is an IIFE (Immediately Invoked Function Expression)? Write an 
// example and explain where it is useful.

/*
IIFE- these functions are the functions which calls the function by call back after the block it 
calls immediately it can also be called as self invoked functions

it can also be called by wrapping ()of the function
*/